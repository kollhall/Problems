#include <iostream>

using namespace std;
int main()
{
    int hi, hf,dia;

    dia = 24;
    scanf("%d %d", &hi, &hf);
    if(hi == hf)
    {
        printf("O JOGO DUROU 24 HORA(S)\n");
    }
        else(a > b)
        {
            printf("O JOGO DUROU %d 24 HORA(S)\n",(dia - hi) + hf ).
        }
            else
            {
                printf("O JOGO DUROU %d HORA(S)\n", hf - hi);
            }
        

    return 0;
}